<div class="container mt-3">
    <div class="row">
        <div class="col-md-8">
            <h2>Daftar Barang</h2>
            <% if (msg.length !== 0 ) { %>
                <div class="alert alert-success fw-bold" role="alert">
                    <%= msg %> 
                </div>
            <% } %>
            <table class="table mt-3">
                <thead>
                    <a href="/stuf/add" class="btn btn-primary float-sm-end btn-sm mt-3"><i class="bi bi-plus-circle"></i>  Tambah Barang </a>
                    <tr>
                      <th scope="col">#</th>
                      <th scope="col">Nama</th>
                      <th scope="col">Warna</th>
                      <th scope="col">Detail</th>
                    </tr>
                  </thead>
                  <tbody>
                    <% if (stufs.length === 0) { %>
                        <tr>
                            <td colspan="4">
                                <div class="alert alert-danger fw-bold" role="alert">
                                    Data barang masih kosong! <br> <a href="/" class="alert-link fw-normal">&laquo; kembali ke halaman utama</a>
                                </div>
                            </td>
                        </tr>
                    <% } %> 
                    <% stufs.forEach((stuf, i) => { i++ %>
                        <tr>
                          <th scope="row"><%= i++ %> </th>
                          <td> <%= stuf.nama %> </td>
                          <td><%= stuf.warna %></td>
                          <td>
                              <a href="/stuf/<%= stuf.nama %> " class="badge rounded-pill bg-primary"><i class="bi bi-info-circle"></i> Detail</a>
                          </td>
                        </tr>                     
                    <% }) %>
                </tbody>
              </table>
        </div>
    </div>
</div>